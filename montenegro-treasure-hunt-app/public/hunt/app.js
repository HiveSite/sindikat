const app=document.querySelector('#app');
const toastEl=document.querySelector('#toast');
const qs=new URLSearchParams(location.search);
const reviewMode=qs.get('review')==='1';
const FINAL={name:'Sastavci - Ribnica and Morača confluence',lat:42.43818,lng:19.25772,radius:110,clue:'Go to the place where two waters meet beneath old stone. Do not search for a monument. Search for the meeting point.'};
const checkpoints=[
 {id:'time',name:'Sahat Kula',lat:42.43595,lng:19.26115,radius:90,chapter:'The hour that stayed behind',clue:'Find the old place where the city once measured every promise in hours.',hint:'Look in the old town for a stone tower built around time.',task:'A note mentions 18:00. Another hand changed one detail later. Which change could make two people both keep the same promise and still never meet?',type:'choice',options:['Changing the hour','Changing the date','Changing the place','Changing their names'],answer:1,fragment:'Marko: “I arrived before six. I stayed until the street went quiet. I thought you had chosen not to come.”',evidence:'He waited. The problem may not have been the promise itself.',points:140},
 {id:'promise',name:'Osmanagić Mosque area',lat:42.43534,lng:19.26035,radius:95,chapter:'The promise',clue:'Find a quiet historic place built for a community, where words once travelled farther than paper.',hint:'Stay in Stara Varoš and look for a historic religious landmark with a minaret.',task:'The recovered sentence is broken into four parts: TOGETHER / WE LEAVE / AFTER SUNSET / TOMORROW. Which part makes this a shared plan rather than a private decision?',type:'choice',options:['TOGETHER','WE LEAVE','AFTER SUNSET','TOMORROW'],answer:0,fragment:'Ana: “You said we would leave together. I packed only what I could carry.”',evidence:'Both expected a shared departure. Neither version begins with a breakup.',points:140},
 {id:'letter',name:'Starodoganjska Mosque area',lat:42.43612,lng:19.26006,radius:95,chapter:'The letter',clue:'Find a place whose old name still carries the memory of records, trade and things that had to be accounted for.',hint:'The clue is in an old word connected with customs and records.',task:'A coded word appears on the back of the envelope: MFUUFS. Move every letter one step backward in the alphabet. What word do you get?',type:'text',answerText:'LETTER',fragment:'Ana: “There was supposed to be one last letter. I never received it.”',evidence:'A missing message sits between their promise and the night they failed to meet.',points:160},
 {id:'safe',name:'Ribnica Fortress / Depedogen',lat:42.43772,lng:19.25712,radius:105,chapter:'What was kept safe',clue:'Find the remains of a place built to control approaches and protect what mattered.',hint:'Think stone, defence and the meeting of old routes near the river.',task:'Inside the story, one object was hidden instead of delivered. Which choice best explains why someone would hide it here?',type:'choice',options:['To destroy the relationship','To keep it safe until the right person came','To sell it later','To prove the letter was fake'],answer:1,fragment:'Marko: “I left the letters where no one would throw them away. I thought you would come back for them.”',evidence:'The letters were preserved, not destroyed. Someone expected a return.',points:150},
 {id:'departure',name:'King’s Park',lat:42.43964,lng:19.26483,radius:105,chapter:'The departure',clue:'Find a formal city garden between movement, memory and the route out of the old centre.',hint:'Look for a landscaped public park close to the city centre.',task:'A damaged ticket shows two seat numbers but only one departure time. What is the strongest conclusion?',type:'choice',options:['Only one person planned to leave','Two people planned the same journey','The tickets were bought on different days','The journey had already happened'],answer:1,fragment:'A booking record carries two seats under the same departure. One was never used.',evidence:'The plan was real and it included both of them.',points:150},
 {id:'waiting',name:'Skaline',lat:42.43915,lng:19.25874,radius:110,chapter:'Two nights of waiting',clue:'Descend toward the river where the city becomes quieter and footsteps replace traffic.',hint:'Look for the famous steps that lead down toward the Ribnica area.',task:'Two notes read: “18:05 - still waiting” and “18:07 - still waiting.” What single missing detail stops us from proving they were there at the same time?',type:'choice',options:['Their names','The weather','The date','The location'],answer:2,fragment:'Two separate notes describe almost the same hour, the same kind of waiting and the same silence.',evidence:'The times match closely. The dates are still unresolved.',points:170},
 {id:'ordinary',name:'Independence Square',lat:42.44138,lng:19.26266,radius:100,chapter:'An ordinary day',clue:'Find the city space where ordinary days, crowds and public life meet in one open square.',hint:'You are looking for the central civic square of Podgorica.',task:'Ana’s list reads: bread, book, coffee, two train tickets, soap. Which item proves she still expected a shared future that day?',type:'choice',options:['Bread','Book','Two train tickets','Soap'],answer:2,fragment:'Her list looked completely ordinary except for one thing: two tickets folded into the same pocket.',evidence:'She did not wake up planning to disappear alone.',points:130},
 {id:'apology',name:'Bokeška Street',lat:42.44215,lng:19.26086,radius:100,chapter:'The unfinished apology',clue:'Find the lively street where conversations spill outside and the city stays awake after dark.',hint:'Look for the central street known for cafés and evening life.',task:'Read the first letter of each line: “Still I waited. / Only your voice mattered. / Returning felt impossible. / Regret followed me. / Years did not change it.” What word appears?',type:'text',answerText:'SORRY',fragment:'Marko never sent the apology. The draft ends after one line: “If you ever learn why I left…”',evidence:'He believed he had something to apologise for, but not for abandoning her.',points:180},
 {id:'twosides',name:'Njegošev Park',lat:42.44255,lng:19.25742,radius:110,chapter:'Two versions of one memory',clue:'Find a green place between the centre and the river, a pause between streets and water.',hint:'A central park near Morača is the next piece.',task:'Ana remembers: “same place, six o’clock, no one came.” Marko remembers: “same place, six o’clock, no one came.” What crucial detail is absent from both memories?',type:'choice',options:['The place','The hour','The date','The promise'],answer:2,fragment:'Their memories agree on the place and hour. They disagree only in a detail neither ever wrote down in the same way.',evidence:'The contradiction is narrowing to one thing: the day itself.',points:180},
 {id:'date',name:'Millennium Bridge',lat:42.44462,lng:19.25849,radius:120,chapter:'The altered date',clue:'Find the modern crossing defined by a tall pylon and cables - a bridge between two sides of the present city.',hint:'Look for Podgorica’s most recognisable modern bridge.',task:'The final message contains “FRI 18” but the paper beneath it carries an erased “SAT 19”. What must the team determine at the finale?',type:'choice',options:['Who bought the tickets','Which date each person actually received','Who arrived first by minutes','Who wrote the first letter'],answer:1,fragment:'The same message existed in two versions. One said Friday the 18th. The other said Saturday the 19th.',evidence:'You now have the final contradiction. Only the meeting place can close the story.',points:200}
];
const state={teamNo:0,code:'',started:false,index:0,score:0,collected:[],hintUsed:{},position:null,watchId:null,screen:'entry',finalReady:false};
function esc(s=''){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function toast(msg){toastEl.textContent=msg;toastEl.classList.add('on');clearTimeout(toastEl._t);toastEl._t=setTimeout(()=>toastEl.classList.remove('on'),2600)}
function teamKey(){return `pg_hunt_v20_team_${state.teamNo}`}
function save(){if(!state.teamNo)return;localStorage.setItem(teamKey(),JSON.stringify({index:state.index,score:state.score,collected:state.collected,hintUsed:state.hintUsed,started:state.started,finalReady:state.finalReady}))}
function restore(){try{const d=JSON.parse(localStorage.getItem(teamKey())||'null');if(d){Object.assign(state,d);return true}}catch{}return false}
function order(){const o=[];for(let i=0;i<checkpoints.length;i++)o.push(checkpoints[(i+state.teamNo-1)%checkpoints.length]);return o}
function current(){return order()[state.index]}
function dist(a,b){const R=6371000,toRad=x=>x*Math.PI/180,dLat=toRad(b.lat-a.lat),dLng=toRad(b.lng-a.lng);const q=Math.sin(dLat/2)**2+Math.cos(toRad(a.lat))*Math.cos(toRad(b.lat))*Math.sin(dLng/2)**2;return 2*R*Math.asin(Math.sqrt(q))}
function entry(){state.screen='entry';app.innerHTML=`<main class="app"><section class="screen"><div class="wrap"><div class="hero"><div class="eyebrow">Podgorica City Treasure Hunt</div><h1>Ten Letters<br>That Never Arrived</h1><p class="muted">Your team will follow ten fragments of the same unfinished relationship across Podgorica. Each team starts from a different point. Everyone reaches the same ending.</p><div class="chips"><span class="chip">10 teams</span><span class="chip">10 rotating chapters</span><span class="chip">1 shared finale</span><span class="chip">100% mobile</span></div></div><div class="card stack"><div><div class="eyebrow">Team access</div><h2>Enter your team code</h2><p class="muted small">Use the code given by the event coordinator. One code = one team route.</p></div><div class="field"><label>Team code</label><input id="teamCode" class="input" placeholder="PG26-01" autocomplete="one-time-code" autocapitalize="characters"></div><button class="btn full" id="enter">Continue</button><div id="err" class="small" style="color:var(--danger);min-height:18px"></div>${reviewMode?'<div class="small muted">Desktop review mode is ON - GPS arrival can be simulated.</div>':'<a class="small muted" href="?review=1">Reviewing from a desktop? Open review mode.</a>'}</div><div class="how"><article><div class="eyebrow">01</div><b>Read the clue</b><span class="small muted">The next place is described, not simply named.</span></article><article><div class="eyebrow">02</div><b>Reach the area</b><span class="small muted">GPS confirms arrival and unlocks the challenge.</span></article><article><div class="eyebrow">03</div><b>Solve together</b><span class="small muted">Every solution unlocks one story fragment.</span></article></div></div></section></main>`;document.querySelector('#enter').onclick=join}
function join(){const code=document.querySelector('#teamCode').value.trim().toUpperCase();const m=code.match(/^PG26-(0[1-9]|10)$/);if(!m){document.querySelector('#err').textContent='Use a team code from PG26-01 to PG26-10.';return}state.teamNo=Number(m[1]);state.code=code;restore();briefing()}
function briefing(){state.screen='brief';app.innerHTML=`<main class="app"><section class="screen"><div class="wrap"><div class="hero"><div class="eyebrow">Team ${String(state.teamNo).padStart(2,'0')}</div><h1>Your route is ready.</h1><p class="muted">You will not receive a list of locations. The story will guide you one step at a time.</p></div><div class="card brief"><div class="eyebrow">Before you start</div><h2>Four things to know</h2><div class="brief-list"><div>📍<span><b>Keep location services on</b><small class="muted">GPS is used only to confirm that your team reached the active area.</small></span></div><div>🧩<span><b>Solve as a team</b><small class="muted">Different challenges reward observation, logic and communication.</small></span></div><div>✉️<span><b>Keep every fragment</b><small class="muted">The story is non-linear. The ending makes sense only after all ten pieces.</small></span></div><div>🚶<span><b>No running is required</b><small class="muted">This is a city challenge, not a race through traffic. Stay on public pedestrian routes.</small></span></div></div><button class="btn full" id="start">${state.started?'Continue game':'Start the story'}</button></div></div></section></main>`;document.querySelector('#start').onclick=()=>{state.started=true;save();startGps();renderGame()}}
function startGps(){if(state.watchId||!navigator.geolocation)return;state.watchId=navigator.geolocation.watchPosition(p=>{state.position={lat:p.coords.latitude,lng:p.coords.longitude,accuracy:p.coords.accuracy};if(state.screen==='game')renderGame(false)},()=>{}, {enableHighAccuracy:true,maximumAge:3000,timeout:12000})}
function distanceLabel(cp){if(!state.position)return 'GPS not ready';const d=Math.round(dist(state.position,cp));if(d<cp.radius)return 'You are here';if(d<250)return `${d} m - very close`;if(d<600)return `${d} m - close`;return `${d} m away`}
function renderGame(scroll=true){state.screen='game';if(state.index>=10){state.finalReady=true;save();return renderFinalClue()}const cp=current(),pct=state.index*10;app.innerHTML=`<main class="app"><header class="topbar"><div class="topin"><div class="teamname">Team ${String(state.teamNo).padStart(2,'0')}<small>Fragment ${state.index+1} of 10</small></div><div class="score">${state.score} pts</div></div><div class="progress"><i style="width:${pct}%"></i></div></header><div class="wrap game"><section class="card mission"><div class="mission-content"><div class="mission-number"><div class="eyebrow">Next chapter</div><span class="distance">${esc(distanceLabel(cp))}</span></div><h1>${esc(cp.chapter)}</h1><div class="clue">“${esc(cp.clue)}”</div><div class="mission-actions"><div class="statusline"><span class="pulse"></span><span>${state.position?'GPS is tracking your position.':'Enable GPS and move toward the clue.'}</span></div><button class="btn full" id="check">Check my position</button>${reviewMode?'<button class="btn ghost full" id="simulate">Simulate arrival for review</button>':''}<button class="btn secondary full" id="hint">I need a hint ${state.hintUsed[cp.id]?'':'(-20 pts)'}</button></div></div></section><nav class="dock"><button id="story"><span>✉️</span>Fragments</button><button id="how"><span>?</span>Help</button><button id="teams"><span>🏁</span>Teams</button></nav></div></main>`;document.querySelector('#check').onclick=()=>checkArrival(cp);if(reviewMode)document.querySelector('#simulate').onclick=()=>openChallenge(cp);document.querySelector('#hint').onclick=()=>showHint(cp);document.querySelector('#story').onclick=showFragments;document.querySelector('#how').onclick=showHelp;document.querySelector('#teams').onclick=showTeams;if(scroll)window.scrollTo(0,0)}
function checkArrival(cp){if(!navigator.geolocation){toast('GPS is not available on this device.');return}navigator.geolocation.getCurrentPosition(p=>{state.position={lat:p.coords.latitude,lng:p.coords.longitude,accuracy:p.coords.accuracy};const d=dist(state.position,cp);if(d<=cp.radius)openChallenge(cp);else{toast(d<250?`Very close - about ${Math.round(d)} m.`:`Keep going - about ${Math.round(d)} m away.`);renderGame(false)}},e=>toast(e.code===1?'Please allow location access to continue.':'Could not read your location.'),{enableHighAccuracy:true,timeout:12000,maximumAge:0})}
function showHint(cp){if(!state.hintUsed[cp.id]){state.score=Math.max(0,state.score-20);state.hintUsed[cp.id]=true;save()}sheet('Location hint',`<div class="hintbox">${esc(cp.hint)}</div><p class="muted" style="color:#536165">A hint helps you find the area. The exact challenge stays locked until arrival.</p>`,[{label:'Got it',action:closeSheet}]);renderGame(false)}
function openChallenge(cp){let selected=null;const body=()=>`<div class="eyebrow" style="color:#9a742a">Location reached</div><h2>${esc(cp.chapter)}</h2><p class="small" style="color:#667378">${esc(cp.name)}</p><p>${esc(cp.task)}</p><div class="question">${cp.type==='choice'?cp.options.map((o,i)=>`<button class="choice" data-choice="${i}">${esc(o)}</button>`).join(''):`<div class="field"><label>Your answer</label><input id="textAnswer" class="input" style="background:#fff;color:#17272c;text-transform:uppercase" autocomplete="off"></div>`}</div>`;sheetRaw(body(),`<button class="btn secondary" id="cancelChallenge">Not yet</button><button class="btn" id="submitChallenge">Submit answer</button>`);document.querySelector('#cancelChallenge').onclick=closeSheet;if(cp.type==='choice')document.querySelectorAll('[data-choice]').forEach(b=>b.onclick=()=>{selected=Number(b.dataset.choice);document.querySelectorAll('[data-choice]').forEach(x=>x.classList.toggle('selected',x===b))});document.querySelector('#submitChallenge').onclick=()=>{const ok=cp.type==='choice'?selected===cp.answer:String(document.querySelector('#textAnswer')?.value||'').trim().toUpperCase()===cp.answerText;if(!ok){toast('Not quite. Look at the clue again and discuss it as a team.');return}complete(cp)}}
function complete(cp){if(state.collected.includes(cp.id)){closeSheet();return}state.score+=cp.points;state.collected.push(cp.id);save();sheetRaw(`<div class="eyebrow" style="color:#3e8a63">Fragment unlocked</div><h2>${esc(cp.chapter)}</h2><div class="fragment">${esc(cp.fragment)}</div><div class="successbox"><b>What this changes</b><p>${esc(cp.evidence)}</p></div><p class="small" style="color:#667378">+${cp.points} points · ${state.collected.length}/10 fragments collected</p>`,`<button class="btn" id="next">Continue</button>`);document.querySelector('#next').onclick=()=>{closeSheet();state.index+=1;save();renderGame()}}
function showFragments(){const list=state.collected.length?state.collected.map(id=>{const cp=checkpoints.find(x=>x.id===id);return `<div class="fragment-card"><b>${esc(cp.chapter)}</b><div class="fragment">${esc(cp.fragment)}</div></div>`}).join(''):'<p>No fragments yet. Reach your first location to begin the story.</p>';sheet('Collected fragments',`<div class="drawer-list">${list}</div>`,[{label:'Close',action:closeSheet}])}
function showHelp(){sheet('How the game works',`<div class="brief-list"><div>1️⃣<span><b>Read the current clue</b><small>Work out the type of place before asking for a hint.</small></span></div><div>2️⃣<span><b>Walk toward it</b><small>The distance indicator becomes more precise as you approach.</small></span></div><div>3️⃣<span><b>Unlock on arrival</b><small>GPS opens the challenge only in the correct area.</small></span></div><div>4️⃣<span><b>Keep the fragment</b><small>Every solved challenge adds one independent piece of Ana and Marko’s story.</small></span></div></div>`,[{label:'Close',action:closeSheet}])}
function showTeams(){const my=state.index*10;const rows=Array.from({length:10},(_,i)=>{const n=i+1;const p=n===state.teamNo?my:Math.max(0,Math.min(100,my+((n*17+state.teamNo*11)%5-2)*10));return `<div class="team-card"><div class="team-row"><b>Team ${String(n).padStart(2,'0')}${n===state.teamNo?' · You':''}</b><div class="bar"><i style="width:${p}%"></i></div><span>${p}%</span></div></div>`}).join('');sheet('Team progress',`<p class="small" style="color:#667378">Progress only. Other teams’ locations and current challenges are never shown.</p><div class="drawer-list">${rows}</div>`,[{label:'Close',action:closeSheet}])}
function renderFinalClue(){state.screen='finalclue';const d=state.position?Math.round(dist(state.position,FINAL)):null;app.innerHTML=`<main class="app"><section class="screen"><div class="wrap final"><div class="eyebrow">10 / 10 fragments collected</div><h1>One place is left.</h1><p class="muted">All ten teams now receive the same final instruction.</p><div class="card"><div class="finalclue">“${esc(FINAL.clue)}”</div><div class="chips" style="justify-content:center"><span class="chip">${d===null?'GPS not ready':d<=FINAL.radius?'Final location reached':`${d} m away`}</span></div><button class="btn full" id="finalCheck" style="margin-top:16px">Check final location</button>${reviewMode?'<button class="btn ghost full" id="finalSim" style="margin-top:9px">Simulate finale for review</button>':''}</div></div></section></main>`;document.querySelector('#finalCheck').onclick=checkFinal;if(reviewMode)document.querySelector('#finalSim').onclick=revealFinal}
function checkFinal(){navigator.geolocation.getCurrentPosition(p=>{state.position={lat:p.coords.latitude,lng:p.coords.longitude,accuracy:p.coords.accuracy};const d=dist(state.position,FINAL);if(d<=FINAL.radius)revealFinal();else{toast(`The ending is still ${Math.round(d)} m away.`);renderFinalClue()}},()=>toast('Allow GPS to unlock the ending.'),{enableHighAccuracy:true,timeout:12000})}
function revealFinal(){state.screen='ending';app.innerHTML=`<main class="app"><section class="screen"><div class="wrap final"><div class="heart">💔</div><div class="eyebrow">The final fragment</div><h1>They both came.</h1><div class="card" style="text-align:left"><p>Marko received the message as <b>Friday the 18th, 18:00</b>.</p><p>Ana received the same promise as <b>Saturday the 19th, 18:00</b>.</p><div class="fragment">They stood at the same meeting place, at the same hour, on different evenings. Each waited alone. Each went home believing the other had chosen not to come.</div><p>The date had been altered between the two versions of the message. Neither abandoned the other. They simply spent the rest of their lives carrying different versions of the same promise.</p><div class="successbox"><b>Final score: ${state.score} points</b><p>${state.collected.length}/10 fragments · Team ${String(state.teamNo).padStart(2,'0')}</p></div></div><button class="btn secondary" id="restart" style="margin-top:14px">Restart this team</button></div></section></main>`;document.querySelector('#restart').onclick=()=>{if(confirm('Restart Team '+String(state.teamNo).padStart(2,'0')+' from the beginning?')){localStorage.removeItem(teamKey());location.reload()}}}
function sheet(title,body,actions=[]){sheetRaw(`<div class="eyebrow" style="color:#9a742a">Podgorica Treasure Hunt</div><h2>${esc(title)}</h2>${body}`,actions.map((a,i)=>`<button class="btn ${i===0&&actions.length>1?'secondary':''}" data-sheet-action="${i}">${esc(a.label)}</button>`).join(''));actions.forEach((a,i)=>document.querySelector(`[data-sheet-action="${i}"]`).onclick=a.action)}
function sheetRaw(body,actions){closeSheet();const root=document.createElement('div');root.id='overlay';root.className='overlay';root.innerHTML=`<section class="sheet"><div class="sheet-head"><div class="handle"></div></div><div class="sheet-scroll">${body}</div><div class="sheet-actions">${actions}</div></section>`;document.body.append(root)}
function closeSheet(){document.querySelector('#overlay')?.remove()}
if('serviceWorker'in navigator)navigator.serviceWorker.register('/hunt/sw.js').catch(()=>{});
entry();